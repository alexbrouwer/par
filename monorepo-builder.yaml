parameters:
  package_alias_format: '<major>.<minor>.x-dev'
  # for "merge" command
  data_to_append:
    require-dev:
      symplify/monorepo-builder: '^7.2'
  
  # for "split" command
  directories_to_repositories:
    packages/core: 'file:///app/gitlocal/core.git'
    packages/time: 'file:///app/gitlocal/time.git'
